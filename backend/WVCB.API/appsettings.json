{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "${POSTGRESQLURL}"
  },
  "JWT": {
    "ValidAudience": "${JWT__ValidAudience}",
    "ValidIssuer": "${JWT__ValidIssuer}",
    "Secret": "${JWT__Secret}"
  },
  "AllowedHosts": "*",
  "AllowedOrigins": [
    "https://wvcbfrontend.z21.web.core.windows.net",
    "http://localhost:4200"
  ]
}
